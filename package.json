{
    "name": "fim-inline",
    "displayName": "FIM Inline Completion",
    "description": "Cost-aware VS Code inline completion using DeepSeek's prefix-and-suffix FIM API.",
    "type": "module",
    "version": "0.3.0",
    "publisher": "xiaodong-hu",
    "repository": "https://github.com/xiaodong-hu/FIM_Inline_Completion",
    "license": "MIT",
    "engines": {
        "vscode": "^1.90.0"
    },
    "categories": [
        "Programming Languages",
        "Machine Learning"
    ],
    "activationEvents": [
        "onStartupFinished",
        "onCommand:FIM.setApiKey",
        "onCommand:FIM.clearApiKey",
        "onCommand:FIM.testRequest",
        "onCommand:FIM.toggle"
    ],
    "main": "./out/extension.js",
    "contributes": {
        "commands": [
            {
                "command": "FIM.setApiKey",
                "title": "FIM: Set API Key"
            },
            {
                "command": "FIM.clearApiKey",
                "title": "FIM: Clear API Key"
            },
            {
                "command": "FIM.testRequest",
                "title": "FIM: Test Request"
            },
            {
                "command": "FIM.toggle",
                "title": "FIM: Toggle Inline Completion"
            }
        ],
        "configuration": {
            "title": "FIM Inline Completion",
            "properties": {
                "FIM.enabled": {
                    "type": "boolean",
                    "default": true,
                    "description": "Enable FIM inline completions."
                },
                "FIM.provider": {
                    "type": "string",
                    "enum": [
                        "deepseek",
                        "custom"
                    ],
                    "default": "deepseek",
                    "description": "Provider preset. Use 'deepseek' for the official DeepSeek hosted API; use 'custom' for an OpenAI-compatible FIM endpoint with prompt/suffix fields."
                },
                "FIM.baseUrl": {
                    "type": "string",
                    "default": "https://api.deepseek.com/beta",
                    "description": "Base URL for the FIM API. For official FIM use `https://api.deepseek.com/beta`. When using a custom provider, change if needed."
                },
                "FIM.completionsPath": {
                    "type": "string",
                    "default": "/completions",
                    "description": "Path appended to `baseUrl` for the completion request."
                },
                "FIM.model": {
                    "type": "string",
                    "default": "deepseek-v4-pro",
                    "description": "Model name sent to the FIM endpoint. DeepSeek FIM Beta supports deepseek-v4-pro with 1M token context window."
                },
                "FIM.apiKeyEnvVar": {
                    "type": "string",
                    "default": "DEEPSEEK_API_KEY",
                    "description": "Environment variable used as fallback when no API key is stored in VS Code SecretStorage."
                },
                "FIM.maxTokens": {
                    "type": "number",
                    "default": 256,
                    "minimum": 1,
                    "maximum": 4096,
                    "description": "Maximum generated tokens for each inline completion. The FIM Beta API supports up to 4K tokens."
                },
                "FIM.temperature": {
                    "type": "number",
                    "default": 0.0,
                    "minimum": 0,
                    "maximum": 2,
                    "description": "Sampling temperature. 0.0 gives deterministic completions (recommended for code)."
                },
                "FIM.topP": {
                    "type": "number",
                    "default": 0.9,
                    "minimum": 0,
                    "maximum": 1,
                    "description": "Nucleus sampling top_p."
                },
                "FIM.requestTimeoutMs": {
                    "type": "number",
                    "default": 30000,
                    "minimum": 100,
                    "description": "HTTP request timeout in milliseconds. Full-document cache-warming requests can take longer than later cache hits."
                },
                "FIM.debounceMs": {
                    "type": "number",
                    "default": 250,
                    "minimum": 0,
                    "description": "Delay before a cache-miss request. Local completion-cache hits bypass this delay."
                },
                "FIM.revalidateDelayMs": {
                    "type": "number",
                    "default": 1500,
                    "minimum": 0,
                    "maximum": 30000,
                    "description": "After part of a cached ghost completion is accepted, wait this long after the last edit before generating a fresh continuation. This avoids one request per accepted word."
                },
                "FIM.stop": {
                    "type": "array",
                    "default": [],
                    "items": {
                        "type": "string"
                    },
                    "description": "Optional stop strings sent to the completion endpoint."
                },
                "FIM.logRequests": {
                    "type": "boolean",
                    "default": false,
                    "description": "Log safe request metadata and DeepSeek-reported prompt cache hit/miss tokens to the FIM output channel. Never logs source text or API keys."
                },
                "FIM.streamEnabled": {
                    "type": "boolean",
                    "default": true,
                    "description": "Enable progressive streaming: show completions as soon as the first chunk of tokens arrives, then continue fetching more in the background for the next keystroke."
                },
                "FIM.streamTokens": {
                    "type": "number",
                    "default": 5,
                    "minimum": 1,
                    "maximum": 64,
                    "description": "Approximate number of tokens to collect before first showing a streamed completion. The stream continues into the content-validated local cache."
                },
                "FIM.localCacheTtlMs": {
                    "type": "number",
                    "default": 300000,
                    "minimum": 1000,
                    "maximum": 3600000,
                    "description": "How long to retain local completions. Entries store hashes of document context rather than copies of the document."
                },
                "FIM.localCacheMaxEntries": {
                    "type": "number",
                    "default": 64,
                    "minimum": 1,
                    "maximum": 512,
                    "description": "Maximum number of content-validated completions retained in memory across documents."
                },
                "FIM.minPrefixChars": {
                    "type": "number",
                    "default": 4,
                    "minimum": 0,
                    "description": "Do not request completions until at least this many non-whitespace prefix characters exist before the cursor."
                }
            }
        }
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "test": "npm run compile && node --test out/*.test.js",
        "package": "vsce package --no-dependencies"
    },
    "devDependencies": {
        "@types/node": "^20.17.0",
        "@types/vscode": "^1.90.0",
        "@vscode/vsce": "^3.2.1",
        "typescript": "^5.6.3"
    }
}
